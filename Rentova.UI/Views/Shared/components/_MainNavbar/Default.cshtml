<nav class="fixed top-0 left-0 right-0 z-50 bg-white/80 backdrop-blur-xl border-b border-brand-gray/30">
    <div class="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between">
        <div class="flex items-center gap-12">
            <a href="/" class="text-2xl font-display font-bold tracking-tighter">
                RENTOVA<span class="text-brand-anthracite">.</span>
            </a>
            <div class="hidden md:flex items-center gap-8 text-sm font-medium text-brand-anthracite">
                <a href="#fleet" class="hover:text-brand-black transition-colors">Filo</a>
                <a href="#brands" class="hover:text-brand-black transition-colors">Markalar</a>
                <a href="#about" class="hover:text-brand-black transition-colors">Hakkımızda</a>
                <a href="#contact" class="hover:text-brand-black transition-colors">İletişim</a>
            </div>
        </div>
        <div class="flex items-center gap-6" id="navActionArea">
            <div id="authButtons" class="flex items-center gap-4">
                <a href="@Url.Action("Index", "Login")" class="inline-flex items-center gap-2 px-5 py-2.5 bg-brand-black text-white rounded-full text-sm font-medium hover:bg-brand-anthracite transition-all">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
                    <span>Giriş Yap</span>
                </a>
            </div>
            <div id="userInfo" class="hidden items-center gap-4">
                @await Component.InvokeAsync("_AccountMenu")
                <span id="userName" class="text-sm font-bold text-brand-black"></span>
                <button onclick="handleLogout()" class="px-4 py-2 bg-red-600 text-white rounded-full text-xs font-bold hover:bg-red-700 transition-all">
                    Çıkış Yap
                </button>
            </div>
            <button class="md:hidden p-2 hover:bg-brand-light rounded-full transition-colors">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="4" x2="20" y1="12" y2="12"/><line x1="4" x2="20" y1="6" y2="6"/><line x1="4" x2="20" y1="18" y2="18"/></svg>
            </button>
        </div>
    </div>
</nav>
